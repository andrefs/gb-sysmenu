#!/bin/bash

# Deactivate root account (andrefs can still run `sudo -s`)
echo
sudo passwd -l root

# Install tools
echo
sudo aptitude update
sudo aptitude safe-upgrade
sudo aptitude install subversion subversion-tools git-core w3m  mlocate sqlite3 screen gcc build-essential
